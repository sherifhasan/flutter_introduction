name: widgets_samples
description: A new Flutter project.
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.4
  dio: ^4.0.6
  geolocator: ^8.2.0
  google_maps_flutter: ^2.1.3
  url_launcher: ^6.0.20
  fluttertoast: ^8.0.9
  hive: ^2.1.0
  hive_flutter: ^1.1.0
  shared_preferences: ^2.0.6

  path_provider: ^2.0.9
dev_dependencies:
  flutter_test:
    sdk: flutter
  hive_generator: ^1.1.1
  build_runner: ^2.1.2
flutter:

  uses-material-design: true
  assets:
    - assets/images/banner_image.jpg