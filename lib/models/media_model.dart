class MediaModel {
  int id, type;
  String image, name, link, linkPart;

  MediaModel(
      {required this.name,
      required this.image,
      required this.id,
      required this.link,
      required this.type,
      required this.linkPart});
}
